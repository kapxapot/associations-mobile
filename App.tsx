import { Box, NativeBaseProvider } from 'native-base';

export default function App() {
  return (
    <NativeBaseProvider>
      <Box>Welcome to Associations Mobile!</Box>
    </NativeBaseProvider>
  );
}
